html 
    head 
        title= title 
    body 
        h1= `Welcome ${username}`   
        br
        if(!log) 
            a(href='/singup')='SINGUP'
            br
            br
            a(href='/login')='LOGIN'
        br
        br
        if (log)
            a(href='/logout')='LOGOUT'